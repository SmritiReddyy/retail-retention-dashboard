# Raw data (large) - but DO commit the small clean parquet for deploy
data/*.xlsx
data/*.csv

# Python
__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# note: data/clean_transactions.parquet IS committed (Streamlit Cloud needs it)
!data/clean_transactions.parquet
